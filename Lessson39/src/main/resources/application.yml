management:
  endpoints:
    web:
      exposure:
        include: '*'
spring:
  mvc:
    view:
      prefix: /pages/
      suffix: .jsp
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:postgresql://localhost:5432/films
    username: postgres
    password: 08102003
